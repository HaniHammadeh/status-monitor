apiVersion: v1
kind: Service
metadata:
  name: status-monitor
  namespace: dev
  labels:
    app: status-monitor
spec:
  type: ClusterIP
  selector:
    app: status-monitor
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP